analyzer:
  exclude:
    - build/**
    - tool/templates/**
  language:
    strict-casts: true
    strict-inference: true

linter:
  rules:
    avoid_print: true
    prefer_final_locals: true
    use_super_parameters: true
