name: Delete the retired drafts bucket

# Remove the studio-drafts audio, once it exists somewhere else.
#
# 98 MB across 33 objects, all belonging to one account: seven original
# recordings and the separated stems beside them. The Studio was retired in
# migration 0066, which deliberately kept the audio because three of those
# recordings never became songs and existed nowhere else.
#
# They exist somewhere else now. `rescue-retired-drafts.yml` downloaded every
# original on 15 September 2026 and they were verified as real audio, byte
# sizes matching the bucket, before this workflow was written. Taylor asked
# for the bucket to go after seeing the list.
#
# This is irreversible, so it is deliberate about three things:
#
#   * it refuses to run unless `confirm` is typed exactly, so a stray click
#     on the Actions tab cannot empty a bucket;
#   * it counts and prints every object before removing anything, so the log
#     is a record of what was there;
#   * it leaves `retired_studio_drafts` alone. Those rows are names,
#     durations and transcripts — a few kilobytes that say what existed, and
#     the whole point of archiving rather than dropping.
#
# The listing has to recurse. The first version of this walked one folder
# deep, found nothing, and the bucket refused to drop as ResourceNotEmpty —
# which is the good failure, but it was still a wrong answer reported as an
# empty bucket. Real paths here are account/draft/file and
# account/draft/stems/kind/file: two to four slashes, not one. So the walk
# goes all the way down, and the run fails loudly if it ends up with nothing
# rather than claiming the bucket was already empty.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "delete the drafts" to confirm'
        type: string
        required: true

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Remove every object in studio-drafts
        env:
          PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          CONFIRM: ${{ inputs.confirm }}
        run: |
          python3 - <<'PY'
          import json, os, sys, urllib.error, urllib.request

          if (os.environ.get("CONFIRM") or "").strip() != "delete the drafts":
              print('Not confirmed. Type "delete the drafts" exactly to run this.')
              sys.exit(1)

          ref = os.environ["PROJECT_REF"]
          key = os.environ["SERVICE_KEY"]
          base = f"https://{ref}.supabase.co"
          bucket = "studio-drafts"
          headers = {
              "apikey": key,
              "Authorization": f"Bearer {key}",
              "Content-Type": "application/json",
          }

          def call(method, url, payload=None):
              request = urllib.request.Request(
                  url,
                  data=json.dumps(payload).encode() if payload is not None else None,
                  method=method,
                  headers=headers,
              )
              with urllib.request.urlopen(request, timeout=120) as response:
                  body = response.read().decode("utf-8", errors="replace")
                  return json.loads(body) if body.strip() else []

          PAGE = 100

          def walk(prefix):
              # A folder comes back with a null id and no metadata; a file has
              # both. Names are relative to the prefix asked for, so the full
              # path is rebuilt on the way down.
              found = []
              offset = 0
              while True:
                  page = call(
                      "POST",
                      f"{base}/storage/v1/object/list/{bucket}",
                      {
                          "prefix": prefix,
                          "limit": PAGE,
                          "offset": offset,
                          "sortBy": {"column": "name", "order": "asc"},
                      },
                  )
                  for entry in page:
                      name = entry.get("name")
                      if not name:
                          continue
                      full = f"{prefix}/{name}" if prefix else name
                      if entry.get("id") is None:
                          found.extend(walk(full))
                      else:
                          found.append(full)
                  if len(page) < PAGE:
                      break
                  offset += len(page)
              return found

          names = walk("")

          if not names:
              print("Found no objects. That is not believable for this bucket,")
              print("so nothing was removed. Check the listing before retrying.")
              sys.exit(1)

          print(f"{len(names)} object(s) to remove:")
          print()
          for name in names:
              print(f"  {name}")
          print()

          # One request per batch, so a partial failure is visible per batch
          # rather than as one opaque error over 33 paths.
          removed = 0
          for start in range(0, len(names), 50):
              batch = names[start : start + 50]
              result = call(
                  "DELETE", f"{base}/storage/v1/object/{bucket}", {"prefixes": batch}
              )
              removed += len(result) if isinstance(result, list) else len(batch)
          print(f"Removed {removed} object(s).")

          # An empty bucket is still a bucket. Remove the bucket itself, so the
          # Studio leaves nothing behind that a future reader has to explain.
          leftover = walk("")
          if leftover:
              print(f"{len(leftover)} object(s) still listed; leaving the bucket:")
              for name in leftover:
                  print(f"  {name}")
              sys.exit(1)

          try:
              call("DELETE", f"{base}/storage/v1/bucket/{bucket}")
              print(f"Removed the {bucket} bucket.")
          except urllib.error.HTTPError as error:
              detail = error.read().decode("utf-8", errors="replace")
              print(f"Objects are gone; the bucket row remains: HTTP {error.code} {detail}")
              sys.exit(1)

          print()
          print(
              "retired_studio_drafts still holds the names, durations and "
              "transcripts. The audio was exported and verified first."
          )
          PY
